name: Prebuild with pdflatex and lwarp
on:
  workflow_dispatch:
  push:
    paths:
      - 'content/**.tex'

jobs:
  build_tex:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: xu-cheng/texlive-action@v3
        with:
         scheme: full
         run: |
          pdflatex -interaction=nonstopmode content/main.tex
          lwarp content/main.tex