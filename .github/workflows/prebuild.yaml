name: Build HTML from TeX

on:
  workflow_dispatch:
  push:
    paths:
      - 'content/**/*.tex'

jobs:
  build_html:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build HTML from LaTeX with lwarp
        uses: xu-cheng/texlive-action@v3
        with:
          texlive_version: latest
          scheme: full
          run: |
            set -euxo pipefail

            if command -v apt-get >/dev/null 2>&1; then
              apt-get update
              apt-get install -y --no-install-recommends poppler-utils
            elif command -v apk >/dev/null 2>&1; then
              apk add --no-cache poppler-utils
            fi

            ROOT="$GITHUB_WORKSPACE"
            TMPROOT="${RUNNER_TEMP:-/tmp}/lwarp-jobs"
            mkdir -p "$TMPROOT"

            find content -type f -name '*.tex' -print0 |
              while IFS= read -r -d '' tex; do
                src_dir="$(dirname "$tex")"
                base="$(basename "$tex" .tex)"

                jobdir="$(mktemp -d "$TMPROOT/${base}-XXXX")"

                cp -a "$src_dir"/. "$jobdir"/

                cd "$jobdir"

                pdflatex -interaction=nonstopmode -halt-on-error "$base.tex"

                lwarpmk html -p "$base"

                mkdir -p "$ROOT/$src_dir"
                find . -maxdepth 1 -type f -name '*.html' -print0 |
                  while IFS= read -r -d '' f; do
                    cp "$f" "$ROOT/$src_dir/"
                  done

                lwarpmk clean -p "$base" || true

                cd "$ROOT"
                rm -rf "$jobdir"
              done
            set -euxo pipefail

            if command -v apk >/dev/null 2>&1; then
              apk add --no-cache poppler-utils
            elif command -v apt-get >/dev/null 2>&1; then
              apt-get update
              apt-get install -y --no-install-recommends poppler-utils
            fi

            find content -name '*.tex' -print0 |
              while IFS= read -r -d '' tex; do
                dir="$(dirname "$tex")"
                base="$(basename "$tex" .tex)"

                cd "$dir"
                pdflatex -interaction=nonstopmode -halt-on-error "$base.tex"
                lwarpmk html "$base.tex"
                cd - >/dev/null
              done
      - name: Upload HTML artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lwarp-html
          path: |
            content/**/*.html
          if-no-files-found: error